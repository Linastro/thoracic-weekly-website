# === supercronic crontab ===
# 注意:supercronic 不支持 && / || 链式命令,每行只能是一个可执行文件(脚本或命令)
# 注意:supercronic **不支持 @reboot 行**(bad crontab line)
# 容器启动时 initial build 由 entrypoint.sh 完成(看 Dockerfile CMD)
# 复杂逻辑放在 /app/cron_jobs/*.sh 脚本里

# Daily 8:00 Beijing (= UTC 0:00) - fetch yesterday's PubMed thoracic literature, then rebuild Astro
0 0 * * * /app/cron_jobs/daily.sh
# Every Monday 10:00 Beijing - cleanup snapshots older than 180 days
0 2 * * 1 /app/cron_jobs/cleanup.sh