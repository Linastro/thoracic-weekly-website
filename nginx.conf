server {
    listen 80 default_server;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 目录补斜杠的 301 若用绝对地址,会按容器内的 80 端口生成 Location,
    # 丢掉宿主映射的端口(如 8080)。发相对 Location 由浏览器补全 origin。
    absolute_redirect off;

    client_max_body_size 10m;

    # Static SPA assets (built Astro output)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Long-cache for fingerprinted static assets
    location ~* \.(css|js|svg|png|ico|webp|jpg|jpeg|gif|woff2?)$ {
        try_files $uri =404;
        expires 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
        access_log off;
    }

    # Reverse proxy: nginx :80/api/*  ->  api:8080/api/*
    location /api/ {
        proxy_pass http://api:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_redirect off;
    }

    # Health check passthrough
    location = /healthz {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}
